import styled from "styled-components"

export const ConvertKitWrapper = styled.div`
  align-items: center;
  display: flex;
  justify-content: center;
  padding: 1rem;

  .seva-custom-content{
    background: var(--background);
    color: var(--postColor);
  }
`